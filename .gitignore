node_modules
.next
.next*/
out
local.db
local.db-*
.local.db
city-*.db
.env
.env.local
.env.*.local
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Locally uploaded profile images (STORAGE_MODE=local). Prod uses Vercel Blob.
public/uploads/*
!public/uploads/.gitkeep

# Private application uploads must never enter source control or a deployment bundle.
.private-uploads/
.vercel
